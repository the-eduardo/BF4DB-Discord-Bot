# Copy to .env (git-ignored).

# Discord bot token: https://discord.com/developers/applications
DISCORD_BOT_TOKEN=

# BF4DB Patreon API key: https://bf4db.com/patreon
BF4DB_API=

# Optional: register slash commands in a single guild (instant) instead of
# globally (up to an hour to propagate).
# DISCORD_GUILD_ID=

# Optional: debug | info | warn | error
# LOG_LEVEL=info

# Optional: how many name-search hits to look up (default 15).
# BF4DB_NAME_LIMIT=15

# Optional: override the API and website roots (proxy, mock).
# BF4DB_BASE_URL=https://bf4db.com/api
# BF4DB_WEB_URL=https://bf4db.com

# Optional: roles allowed to search by IP, on top of members who can manage the
# server. Comma-separated role ids.
# BF4DB_IP_ROLE_IDS=

# Optional: Uptime Kuma push monitor (dead-man switch). The bot only pushes
# while the Discord gateway is connected.
# KUMA_PUSH_URL=http://uptime-kuma:3001/api/push/XXXXXXXX
